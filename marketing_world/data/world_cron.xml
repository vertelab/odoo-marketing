<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- World Monitor Pull — every 30 minutes -->
    <record id="cron_world_monitor_pull" model="ir.cron">
        <field name="name">World Monitor Pull</field>
        <field name="model_id" ref="model_marketing_world_event"/>
        <field name="state">code</field>
        <field name="code">model.cron_pull_events()</field>
        <field name="interval_number">30</field>
        <field name="interval_type">minutes</field>
        <field name="user_id" ref="base.user_root"/>
        <field name="active">True</field>
        <field name="priority">5</field>
    </record>

    <!-- World Intelligence Triage — every 6 hours -->
    <record id="cron_world_intelligence_triage" model="ir.cron">
        <field name="name">World Intelligence Triage</field>
        <field name="model_id" ref="model_marketing_world_event"/>
        <field name="state">code</field>
        <field name="code">model.cron_triage_events()</field>
        <field name="interval_number">6</field>
        <field name="interval_type">hours</field>
        <field name="user_id" ref="base.user_root"/>
        <field name="active">True</field>
        <field name="priority">6</field>
    </record>

    <!-- World Brief Generator — weekly Monday 08:00 -->
    <record id="cron_world_brief_generator" model="ir.cron">
        <field name="name">World Brief Generator</field>
        <field name="model_id" ref="model_marketing_world_brief"/>
        <field name="state">code</field>
        <field name="code">model.cron_generate_briefs()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">weeks</field>
        <field name="user_id" ref="base.user_root"/>
        <field name="active">True</field>
        <field name="priority">7</field>
        <field name="nextcall" eval="(DateTime.now().replace(hour=8, minute=0, second=0) + relativedelta(days=(7 - DateTime.now().weekday()) % 7 or 7)).strftime('%Y-%m-%d %H:%M:%S')"/>
    </record>

    <!-- World Report Generator — daily check -->
    <record id="cron_world_report_generator" model="ir.cron">
        <field name="name">World Report Generator</field>
        <field name="model_id" ref="model_marketing_world_report"/>
        <field name="state">code</field>
        <field name="code">model.cron_generate_reports()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="user_id" ref="base.user_root"/>
        <field name="active">True</field>
        <field name="priority">8</field>
        <field name="nextcall" eval="(DateTime.now() + relativedelta(hours=2)).strftime('%Y-%m-%d %H:%M:%S')"/>
    </record>
</odoo>
